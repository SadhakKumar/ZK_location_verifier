import { z } from "zod";


/**
 * Action schemas for the location action provider.
 *
 * This file contains the Zod schemas that define the shape and validation
 * rules for action parameters in the location action provider.
 */


/**
 * Schema for the getLocationName action.
 * This action doesn't require any parameters as the contract address is hardcoded.
 */
export const GetLocationSchema = z.object({});








